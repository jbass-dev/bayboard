rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: any signed-in user can read/write.
    // Tighten per-collection in the stretch phase (role-based views).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
